# Parse grade level strings to standard format

Converts CDE grade level strings to standard format (PK, K, 01-12,
TOTAL).

## Usage

``` r
parse_grade_level(grade_raw)
```

## Arguments

- grade_raw:

  Character vector of raw grade level strings

## Value

Character vector of standardized grade levels
