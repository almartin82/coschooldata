# Create state-level aggregates from district data

Create state-level aggregates from district data

## Usage

``` r
create_state_aggregates_from_districts(district_tidy)
```

## Arguments

- district_tidy:

  Long format district-level data

## Value

Data frame of state-level tidy rows
