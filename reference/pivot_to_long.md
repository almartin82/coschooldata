# Pivot wide data to long format

Pivot wide data to long format

## Usage

``` r
pivot_to_long(df, subgroups)
```

## Arguments

- df:

  Wide data frame

- subgroups:

  Character vector of subgroup column names

## Value

Long format data frame
